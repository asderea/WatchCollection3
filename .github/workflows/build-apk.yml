name: Build Watch Collection APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android SDK 36
        run: |
          sdkmanager "platforms;android-36"
          sdkmanager "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '8.13'

      - name: Check project structure
        run: |
          echo "===== Repository root ====="
          ls -la

          echo "===== WatchCollection ====="
          ls -la WatchCollection

          test -f WatchCollection/settings.gradle
          test -f WatchCollection/build.gradle
          test -f WatchCollection/app/build.gradle

      - name: Build debug APK
        working-directory: WatchCollection
        run: gradle :app:assembleDebug --stacktrace

      - name: Find generated APK
        run: |
          find WatchCollection -type f -name "*.apk" -print
          test -f WatchCollection/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v6
        with:
          name: WatchCollection-v1.4-debug-apk
          path: WatchCollection/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
